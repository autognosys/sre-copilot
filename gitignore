# --- Secrets & credentials ---
.env
.env.*
*.pem
*.key
*_rsa
*_rsa.pub
secrets/
**/secrets.yaml
**/secrets.yml
credentials.json
*token*
!*token*.example

# --- Terraform ---
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# --- Ansible ---
*.retry
vault_pass.txt
inventory/host_vars/**/vault.yml
inventory/group_vars/**/vault.yml

# --- Kubernetes / kubeconfig ---
kubeconfig
kubeconfig.*
*.kubeconfig
**/kube-config*

# --- Slack / webhook config actually used at runtime ---
config/local.yaml
config/local.yml
webhook_url.txt

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# --- Local model weights / large binaries (keep repo lean; document how to fetch instead) ---
*.gguf
*.bin
models/*.safetensors

# --- Logs & local run artifacts ---
*.log
logs/
.DS_Store

# --- IDE ---
.vscode/
.idea/
